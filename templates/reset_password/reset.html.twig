{% extends 'base.html.twig' %}

{% block title %}Resetowanie hasła{% endblock %}

{% block body %}
    <h1 class="fw-bold my-4">Zresetuj hasło</h1>

    {% form_theme resetForm 'bootstrap_5_layout.html.twig' %}
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Zresetuj hasło</button>
    {{ form_end(resetForm) }}
{% endblock %}
