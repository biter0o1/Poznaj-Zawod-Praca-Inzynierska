{% extends 'base.html.twig' %}

{% block title %}Example{% endblock %}

{% block body %}
    <h1>Example</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ example.id }}</td>
            </tr>
            <tr>
                <th>ExampleString</th>
                <td>{{ example.exampleString }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_example_index') }}">back to list</a>

    <a href="{{ path('app_example_edit', {'id': example.id}) }}">edit</a>

    {{ include('example/_delete_form.html.twig') }}
{% endblock %}
